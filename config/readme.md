# Config Directory
This directory contains configuration files for the launch controller system.

## Content
- default: Directory containing the default config files.
- old_launch_controller_test: Directory containing config files for general purpose testing with the old launch controller.